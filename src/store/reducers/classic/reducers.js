export const firstReducer = (state = { name:'Vladimir', lastname:'Pavković' }, action)=>{
    return state;
}

export const secondReducer = (state = { ime:'Pavle', prezime:'Pavković' }, action)=>{
    return state;
}

export const thirdReducer = (state = { ime2:'Nataša', prezime2:'Pavković' }, action)=>{
    return state;
}
